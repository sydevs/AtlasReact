import type { StoryDefault } from '@ladle/react'

import { LanguageSelector } from '@/components/atoms'

export default { title: 'Atoms / Language Selector' } satisfies StoryDefault

export const Default = () => <LanguageSelector />
